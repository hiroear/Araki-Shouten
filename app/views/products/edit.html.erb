<!--商品編集ページ-->
<!--rails側はフォームの状態によってどのリクエストなのかを自動的に判断する。-->
<!--フォームに初期値があり、かつパラメータを渡した状態でsubmitすれば、自動的にPUTもしくはPATCHリクエストだと判断される-->

<div class="container">
  <h1>商品情報更新</h1>

  <%= form_with model: @product do |f| %>
    <div class="form-group">
      <%= f.label "商品名" %>
      <%= f.text_field :name, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label "商品説明" %>
      <%= f.text_area :description, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label "価格" %>
      <%= f.number_field :price, class: "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label "カテゴリ" %>
      <%= f.select :category_id, @categories.map { |category| [category.name, category.id] }, {}, class: "form-control" %>
    </div>
    <%= f.submit "更新", class: "btn btn-danger" %>
  <% end %>

  <%= link_to "商品一覧に戻る", products_path %>
</div>
